# 초기 검증 기록

검증일: 2026-09-09. 소스 기능은 변경하지 않았습니다.

## 원본과 환경

- 원본: `D:\배포\LoadMonitor24_풀패키지_20260909_172407.zip`
- 크기: 12,215,307 bytes
- SHA-256: `2db12eee37365904d1a5228c1dda30740ba65f119d8253885e1a87aa59ec020f`
- ZIP: 파일 101개 + 빈 폴더 항목 2개. 경로 유효성을 확인한 후 새 폴더에 해제했습니다.
- 추출 파일 101개 모두 ZIP 파일 내용과 SHA-256 일치. 기존 `D:\배포\loadmon24`의 대응 파일도 101개 모두 일치했습니다.
- 내장 Python: 3.11.9. 개발 Python의 Ruff: 0.16.3. Node.js와 Git 사용 가능.
- 검토한 기존 Markdown: 22개. 개별 파일 해시는 [BASELINE.json](BASELINE.json)에 있습니다.

## 검사 결과

| 검사 | 결과 |
|---|---|
| 필수 파일 자가점검 | 67/67 존재 |
| FILES.txt 크기·CRC32 | 67개 목록 일치 |
| Python AST 문법 | 35개 통과 |
| 설정 JSON 문법 | 3개 통과 |
| 기존 `tools/lint.ps1` | 7관문 통과: Ruff, PowerShell 파싱, PAGE 개행, BAT/PS1 인코딩, JS 문법, 배포 경로 흔적 |
| 별도 UI Ruff | 기존 W605 경고 4건으로 실패 |
| PAGE/TEAM_PAGE 실제 문자열의 JS | Node 파서 통과 |

별도 UI 검사를 추가한 이유: 기존 린트의 Ruff 대상은 `core collect tools *.py`이므로 `ui/app.py`가 포함되지 않습니다.

## 원본에서 발견한 경고

`LoadMonitor24/ui/app.py:3228`의 Python 다중행 HTML/JavaScript 문자열에 `\-`, `\[`, `\]`, `\s`가 각각 한 번 있어 Ruff W605가 4건 발생합니다. 현재 Python 3.11의 AST와 결과 JavaScript 문법은 통과했습니다. 원본 기준을 보존하기 위해 이 준비 작업에서 수정하지 않았습니다.

후속 수정 시에는 Python 소스의 역슬래시를 적절히 이스케이프하되 브라우저로 전달되는 정규식은 동일하게 유지하고, `FILES.txt` 갱신 후 검증기를 실행해야 합니다. 현재 기본 검증기의 종료 코드는 이 4건 때문에 **1**입니다.

## 검증 범위

검사는 TEMP의 코드·기본설정 복제본에서 수행했습니다. 원본/새 프로젝트의 `data`, `report`, `teamdata`, 개인 설정과 로그인 프로필을 테스트 입력으로 사용하지 않았습니다. TEMP 복제본과 로그는 재확인을 위해 남깁니다.

로그: `verification/check-20260909-220354-512.txt`.

UI 서버 시작, 수집, 스케줄러 등록, 실제 Copilot·Outlook·Teams 연결, 보고서 E2E 생성, 팀 전송은 수행하지 않았습니다. 기능 회귀 스위트는 지정 ZIP에 포함되어 있지 않습니다. 이 결과는 초기 파일·정적 검증 기록입니다.
